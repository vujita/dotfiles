[alias]
	co = checkout
	br = branch
	ci = commit
	st = status
[user]
	email = shardphoneix@gmail.com 
	name = Vu Nguyen
[core]
  pager = delta
	excludesfile = ~/.gitignore_global
	editor = nvim
[init]
	defaultBranch = main
[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n and N to move between diff sections
    light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)

[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default
[commit]
    gpgsign = false
[pull]
	rebase = true
